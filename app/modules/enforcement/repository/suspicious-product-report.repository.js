import { BaseRepository } from '../../../shared/repository/base.repository.js';

export class SuspiciousProductReportRepository extends BaseRepository {
  constructor({ Model }) {
    super({ Model });
  }
}

import { BaseRepository } from '../../../shared/repository/base.repository.js';

export class ReconciliationItemRepository extends BaseRepository {
  constructor({ Model }) {
    super({ Model });
  }
}

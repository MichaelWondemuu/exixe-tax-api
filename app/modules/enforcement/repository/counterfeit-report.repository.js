import { BaseRepository } from '../../../shared/repository/base.repository.js';

export class CounterfeitReportRepository extends BaseRepository {
  constructor({ Model }) {
    super({ Model });
  }
}

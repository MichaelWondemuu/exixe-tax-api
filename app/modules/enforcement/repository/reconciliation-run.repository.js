import { BaseRepository } from '../../../shared/repository/base.repository.js';

export class ReconciliationRunRepository extends BaseRepository {
  constructor({ Model }) {
    super({ Model });
  }
}
